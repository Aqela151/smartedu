import 'package:flutter/material.dart';

class ChitchatPage extends StatelessWidget {
  const ChitchatPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('Chitchat Page')),
    );
  }
}
