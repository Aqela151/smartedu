// taskpost_page.dart
import 'package:flutter/material.dart';

class GradeviewPage extends StatelessWidget {
  const GradeviewPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('Gradeview Page')),
    );
  }
}
